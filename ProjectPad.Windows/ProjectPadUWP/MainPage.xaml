<Page
    x:Class="ProjectPadUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjectPadUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Vertical" Visibility="{x:Bind ToVisible(DataObject.HasToken), Mode=OneWay}" Background="#f5f5f5">
            <Button x:Name="btnDeconnexion" Click="btnDeconnexion_Click">Déconnexion</Button>
        </StackPanel>
        <Grid Visibility="{x:Bind ToNoyVisible(DataObject.HasToken), Mode=OneWay}" Background="#efefef">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image Source="logo-ops-noir.png" VerticalAlignment="Center" MaxWidth="640" HorizontalAlignment="Center" />
                <TextBlock Text="Use The Ops - Project Pad" HorizontalAlignment="Center" />
                <Button HorizontalAlignment="Center" x:Name="btnConnexion" Click="btnConnexion_Click">Connexion</Button>
            </StackPanel>
        </Grid>
        

    </Grid>
</Page>
